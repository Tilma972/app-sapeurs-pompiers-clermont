// Deprecated component: not used anymore. Kept as a stub to avoid accidental imports.
export function PartnersSection() {
  if (typeof window !== 'undefined' && process.env.NODE_ENV !== 'production') {
    console.warn('[Deprecated] components/landing/partners-section.tsx is deprecated. Use Partenaires.tsx instead.');
  }
  return null;
}
