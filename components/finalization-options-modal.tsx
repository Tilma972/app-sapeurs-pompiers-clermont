"use client"

// Stub component: the deferred donor completion flow has been retired.
// Kept only to avoid breaking imports while we phase out the file.
export function FinalizationOptionsModal() {
  return null
}
